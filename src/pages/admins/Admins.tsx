import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const Admins: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(Admins);