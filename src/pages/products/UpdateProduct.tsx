import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const UpdateProduct: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(UpdateProduct);