import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const AddFaqs: FC<IProps> = ({  }) => {
  return (
    <>
    add

    </>
  );
}

export default memo(AddFaqs);