import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const AddressBook: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(AddressBook);