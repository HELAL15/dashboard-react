import { FC, memo } from "react";

/**
 * ==> props interface
 */
interface IProps {

}

/**
 * ==> Component
 */
const PaymentOption: FC<IProps> = ({  }) => {
  return (
    <>

    </>
  );
}

export default memo(PaymentOption);